# R20 — Réduire l'impact des requêtes silencieuses via CSP

**Explication** : Certaines directives CSP (`connect-src`, `prefetch-src`) contrôlent les requêtes réseau silencieuses (fetch, XHR, prefetch). Sans restriction, un script malveillant pourrait exfiltrer des données vers un serveur tiers via ces canaux. La politique CSP doit restreindre ces sources au strict nécessaire.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
