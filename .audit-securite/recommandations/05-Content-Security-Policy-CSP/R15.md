# R15 — Interdire des contenus inline

**Explication** : La directive CSP doit interdire l'exécution de scripts et styles inline (`'unsafe-inline'` absent). Cela bloque tout JavaScript inline (attributs `on*`, balises `<script>` sans `src`) et CSS inline. C'est la mesure la plus efficace contre les XSS, car même si un attaquant injecte du code, le navigateur refuse de l'exécuter.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
