# R14- — Mettre en oeuvre CSP par balise meta dans les pages HTML

**Explication** : Variante dégradée de R14. Si l'en-tête HTTP n'est pas réalisable, la balise `<meta http-equiv="Content-Security-Policy" content="...">` peut être utilisée dans chaque page HTML. Limitation : elle ne protège pas les réponses non-HTML (JSON, XML) et certaines directives (`frame-ancestors`, `report-uri`) ne sont pas supportées via meta.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
