# R17 — Utiliser CSP contre le clickjacking

**Explication** : La directive CSP `frame-ancestors` permet de contrôler quels sites peuvent embarquer la page dans une iframe. `frame-ancestors 'self'` autorise uniquement le même domaine. Cette directive remplace et améliore `X-Frame-Options` pour la protection contre le clickjacking (attaque où un site malveillant superpose une iframe invisible).

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
