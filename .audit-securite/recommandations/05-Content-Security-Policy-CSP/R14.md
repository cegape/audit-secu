# R14 — Mettre en oeuvre CSP par en-tête HTTP

**Explication** : L'en-tête HTTP `Content-Security-Policy` est le mécanisme de défense en profondeur principal contre les attaques XSS. Il définit une liste blanche de sources autorisées pour chaque type de ressource (scripts, styles, images, polices, etc.). La mise en oeuvre par en-tête HTTP est préférable car elle couvre toutes les réponses du serveur.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
