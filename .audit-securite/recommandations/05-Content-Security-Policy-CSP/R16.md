# R16 — Définir la directive default-src

**Explication** : La directive `default-src` dans CSP sert de politique par défaut pour tous les types de ressources non explicitement couverts. Elle doit être définie de manière restrictive (idéalement `'self'` ou `'none'`) pour garantir qu'aucune source non autorisée ne soit implicitement permise.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
