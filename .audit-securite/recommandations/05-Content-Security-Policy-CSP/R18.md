# R18 — Utiliser X-Frame-Options contre le clickjacking

**Explication** : L'en-tête `X-Frame-Options` empêche l'inclusion de la page dans une iframe sur un site tiers. Les valeurs possibles sont `DENY` (interdit toute iframe) ou `SAMEORIGIN` (autorise uniquement le même domaine). Cet en-tête est un complément à `frame-ancestors` de CSP pour les navigateurs plus anciens.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
