# R6 — Expliciter la nature d'une ressource avec l'en-tête Content-Type

**Explication** : Chaque réponse HTTP doit contenir un en-tête `Content-Type` explicite et correct (ex : `text/html; charset=utf-8`, `application/json`). Sans cet en-tête, le navigateur tente de deviner le type MIME (MIME sniffing), ce qui peut mener à l'interprétation d'un fichier texte comme du code exécutable. L'en-tête `X-Content-Type-Options: nosniff` doit compléter cette mesure.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
