# R47 — Utiliser le mode strict

**Explication** : La directive `"use strict"` active le mode strict de JavaScript qui élimine certains comportements dangereux : interdiction des variables globales implicites, levée d'erreurs pour les affectations en lecture seule, interdiction de `with`, restriction de `eval()`. Il doit être activé globalement ou par module.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
