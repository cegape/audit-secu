# R45 — Sécuriser l'ouverture de nouvelles fenêtres

**Explication** : Les liens ouvrant une nouvelle fenêtre (`target="_blank"`) doivent inclure l'attribut `rel="noopener noreferrer"`. Sans `noopener`, la page ouverte a accès à l'objet `window.opener` de la page d'origine, ce qui permet des attaques de type tabnabbing (la page d'origine est redirigée vers un site de phishing).

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
