# R25 — Proscrire l'usage de l'API Web SQL Database

**Explication** : L'API Web SQL Database est obsolète (dépréciée par le W3C). Elle n'est plus maintenue, présente des vulnérabilités connues (injection SQL côté client) et n'est supportée que par certains navigateurs. Son usage doit être totalement interdit au profit d'IndexedDB si un stockage structuré côté client est nécessaire.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
