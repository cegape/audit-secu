# R22 — Modifier ponctuellement l'en-tête Referer

**Explication** : Pour des liens spécifiques vers des sites externes, l'attribut `referrerpolicy` sur les éléments `<a>`, `<area>`, `<img>`, `<iframe>`, `<link>` ou `<script>` permet de surcharger la politique globale. Cela permet d'appliquer une politique plus restrictive (`no-referrer`) pour les liens vers des tiers sans impacter la navigation interne.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
