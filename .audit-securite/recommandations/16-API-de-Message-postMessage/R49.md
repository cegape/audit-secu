# R49 — Formaliser les échanges en utilisant l'API de Message

**Explication** : Les communications entre contextes d'exécution différents (fenêtres, iframes, Workers) doivent utiliser l'API `postMessage`. Cette API permet un échange structuré et contrôlé. Les données transmises doivent être sérialisables et ne pas contenir de références directes à des objets sensibles.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
