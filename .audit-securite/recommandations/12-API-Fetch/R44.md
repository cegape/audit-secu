# R44 — Préférer l'utilisation de l'API Fetch à XMLHttpRequest

**Explication** : L'API Fetch est l'API moderne pour les requêtes réseau. Elle offre un modèle basé sur les Promises, un contrôle plus fin des requêtes (mode, credentials, redirect), et une meilleure intégration avec les Service Workers. Elle est plus sûre par défaut : pas d'envoi de cookies cross-origin sans `credentials: 'include'` explicite.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
