# R37 — Compléter la mise en oeuvre de XHR par une configuration CSP

**Explication** : La directive CSP `connect-src` doit restreindre les destinations autorisées pour les requêtes XHR/Fetch. Cela empêche un script malveillant d'exfiltrer des données vers un serveur externe. `connect-src 'self'` autorise uniquement les requêtes vers le même origin.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
