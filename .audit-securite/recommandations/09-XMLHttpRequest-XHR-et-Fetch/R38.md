# R38 — Protéger les appels XHR par un contrôle anti-CSRF

**Explication** : Les requêtes XHR modifiant l'état du serveur doivent être protégées contre les attaques CSRF (Cross-Site Request Forgery). Le mécanisme recommandé est un token CSRF synchronisé : un jeton unique est généré côté serveur, inclus dans la page, et renvoyé dans un en-tête custom (ex : `X-CSRF-Token`) à chaque requête XHR.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
