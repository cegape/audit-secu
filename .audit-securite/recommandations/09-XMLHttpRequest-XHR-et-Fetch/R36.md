# R36 — Utiliser XHR avec la méthode POST

**Explication** : Les requêtes XHR modifiant l'état du serveur doivent utiliser POST (ou PUT/DELETE selon la sémantique). POST transmet les données dans le corps de la requête (non visible dans l'URL). C'est la méthode recommandée pour l'envoi de formulaires et les opérations d'écriture.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
