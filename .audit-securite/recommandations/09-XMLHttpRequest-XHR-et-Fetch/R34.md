# R34 — Encoder les réponses XMLHttpRequest

**Explication** : Les réponses reçues via XHR doivent être traitées avec le type de contenu approprié (responseType). Le contenu ne doit jamais être injecté directement dans le DOM sans échappement. La propriété `responseText` insérée via `innerHTML` est un vecteur XSS classique ; préférer `responseJSON` ou `JSON.parse()` puis insertion sécurisée.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
