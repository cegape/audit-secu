# R36- — Utiliser XHR avec la méthode GET sous certaines conditions

**Explication** : Les requêtes XHR en GET sont acceptables uniquement pour la lecture de données. Elles ne doivent pas transmettre de données sensibles dans l'URL (les paramètres GET sont visibles dans les logs serveur, l'historique navigateur et le Referer). Pour les données sensibles, utiliser POST.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
