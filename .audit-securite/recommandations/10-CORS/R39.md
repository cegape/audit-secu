# R39 — Mettre en oeuvre un preflight lors des appels CORS

**Explication** : Les requêtes CORS doivent déclencher un preflight (requête OPTIONS) pour les méthodes ou en-têtes non simples. Le serveur doit répondre avec des en-têtes `Access-Control-Allow-Origin`, `Access-Control-Allow-Methods` et `Access-Control-Allow-Headers` restrictifs. Le wildcard `*` ne doit pas être utilisé si des credentials (cookies) sont transmis.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
