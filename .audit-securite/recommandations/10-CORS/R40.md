# R40 — Vérifier la valeur de l'Origin lors de la réception d'une requête CORS

**Explication** : Le serveur doit vérifier l'en-tête `Origin` de chaque requête CORS et ne répondre avec `Access-Control-Allow-Origin` que si l'origine est dans une liste blanche explicite. Renvoyer `Access-Control-Allow-Origin: *` ou refléter aveuglément l'Origin de la requête est dangereux et permet les attaques cross-origin.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
