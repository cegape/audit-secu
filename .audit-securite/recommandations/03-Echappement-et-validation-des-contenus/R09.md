# R9 — Proscrire l'usage de la fonction eval()

**Explication** : La fonction `eval()` exécute une chaîne de caractères comme du code JavaScript. Son utilisation est un vecteur d'injection de code majeur. Elle doit être totalement proscrite dans le code applicatif. Les alternatives à `eval()` existent pour chaque cas d'usage légitime (JSON.parse, Function constructor encadré, etc.).

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
