# R56 — Compléter la déclaration d'une API de messages par la définition d'une CSP

**Explication** : Si l'application utilise `postMessage`, la politique CSP doit restreindre les sources de frames (`frame-src`, `child-src`) et les sources de workers (`worker-src`) pour garantir que seuls les contextes autorisés peuvent communiquer via l'API de Message.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
