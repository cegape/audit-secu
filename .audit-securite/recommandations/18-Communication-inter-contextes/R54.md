# R54 — Définir l'Origin lors de l'utilisation de l'API de Message

**Explication** : Lors de l'envoi d'un message via `postMessage`, le second paramètre (`targetOrigin`) doit être défini explicitement avec l'origin attendu du destinataire (ex : `"https://app.example.com"`). Ne jamais utiliser `"*"` comme targetOrigin, car cela permet à n'importe quelle page de recevoir le message.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
