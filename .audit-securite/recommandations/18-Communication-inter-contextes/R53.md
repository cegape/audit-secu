# R53 — Formaliser les échanges en utilisant l'API de Message

**Explication** : (Complément de R49 pour le contexte iframes) Les échanges entre la page hôte et les iframes doivent passer exclusivement par `postMessage`. Aucun accès direct aux propriétés des frames cross-origin ne doit être tenté. Le format des messages doit être documenté et validé.

**Statut** : 🔍 À vérifier
**Vérification** : 🔍 À vérifier

**Constat** :

**Preuves/Fichiers** :
